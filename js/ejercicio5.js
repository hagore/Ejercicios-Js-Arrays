//Conteo de elementos
//Dado un arreglo de strings, retorna cuántos elementos tiene el arreglo.

function contarElementos(arr) {
    return arr.length;
  }
  
  // Ejemplo de uso:
  let arreglo = ["manzana", "banana", "naranja"];
  console.log(contarElementos(arreglo));  // 3
  